{
    "name": "{{node_name}}",
    "version": "{{node_version}}",
    "description": "{{node_description}}",
    "main": "{{node_name}}.js",
    "node-red" : {
        "nodes": {
            "{{node_name}}": "{{node_name}}.js"
        }
    }
}